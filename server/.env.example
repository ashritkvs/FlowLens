# FlowLens Express API
# Copy to .env and set for local or production (Render/Railway).

PORT=4000
# MongoDB: local Docker or Atlas connection string
MONGODB_URI=mongodb://localhost:27017/flowlens
# Allowed origin for CORS (frontend URL). Use * for dev; set to Vercel URL in production.
CLIENT_ORIGIN=http://localhost:5173
